{
  "name": "@law-ai/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types, DTOs (zod) and constants used by both FE and BE",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist .turbo .tsbuildinfo"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@law-ai/config": "workspace:*",
    "typescript": "^5.4.5"
  }
}
